module Api
	class FriendshipsController < ApplicationController
	  def create
	  # 	  @friendships = Friendship.create(from_user_id: params[:from_user_id], to_user_id: params[:to_user_id])
	  # 	  # binding.pry
		 #  	if @friendships.save
			#   	render json: @friendships.to_json
			# else
	  #           render json: {message: "create error"}.to_json, status:400
			# end
	  end
	end
end